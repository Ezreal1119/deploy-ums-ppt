# 2026-07-03

## UMS PPT Layout Fixes (index.html)

- **Slide 2 (S22)**: Changed `.image-hero-body` grid from `6fr 6fr` to `5fr 7fr`, gap from `3vw` to `4vw`. Removed `max-width:48ch` from text block to fix right-side whitespace issue — content now distributes more evenly.
- **Slide 3 (S08)**: Reduced header-to-content gap. Grid gap `2vh` → `1vh`, duo-compare inline `margin-top` `4vh` → `2vh`, compact-top CSS override `margin-top:2vh!important` → `margin-top:1vh!important`. Total gap reduced from ~4vh to ~2vh.
- **Slide 4 (S19)**: Title changed to "Pain Points in Device Management" (was "Core Challenges in Enterprise Device Management"). Now single line, layout more compact.
- **Slide 3 (S08) follow-up**: Title shortened to "Two deployment options". Removed `margin-top:auto` from `.duo-compare .col-list` (now `margin-top:1vh;padding-top:1vh`), reduced column gap `1.6vh` → `1.2vh`, header gap `1.4vh` → `1vh`, and changed grid rows from `auto 1fr` to `auto auto` so content no longer stretches. Bullet lists now sit directly below descriptions with minimal whitespace.
- **Slide 3 (S08) second follow-up**: Added `flex:1` to `.duo-compare .col-desc` so both descriptions stretch equally and the horizontal divider lines align across columns. Eliminated remaining top whitespace by setting grid gap `0`, header gap `0.5vh`, and duo-compare `margin-top:0!important`.
- **Slide 3 (S08) final restructure**: Replaced the flex-based column layout with CSS Grid (`.duo-compare-grid`). Descriptions and bullet lists now live in separate grid rows with a single spanning horizontal divider, removing the stretching whitespace while keeping the divider perfectly aligned. Added `.tight-s08` overrides to compress canvas-card gap (`2.4vh` → `0.5vh`) and chrome-min margin-bottom (`32px` → `1vh`). Updated `duo-mirror` animation to use `querySelectorAll` and animate the new divider + lists.
- **Slide 3 (S08) revision**: Removed the ugly spanning divider. Lists moved back inside `.dc-col` so they sit directly below descriptions with `margin-top:0`. Grid simplified to single row. Column internal gap `1vh` → `0.5vh`, header gap `0.5vh` → `0`, canvas-card gap `0.5vh` → `0`, chrome-min margin-bottom `1vh` → `0`, padding-top `3.8vh` → `2vh`. Animation reverted to original single left-col/right-col slide-in.
- **Slide 3 (S08) centering**: Changed content grid to `align-content:center` with `gap:1vh` between title and columns. Set canvas-card padding to `4vh 5vw` (equal top/bottom) so the whole block is vertically centered on the slide.
- **Slide 3 (S08) balance**: Removed `align-content:center` to keep title at top. Restored comfortable spacing: canvas-card padding `5vh 5vw 4vh`, chrome-min margin-bottom `1vh`, content grid gap `2.5vh`, header gap `0.5vh`, column gap `1.2vh`, list margin-top `1.2vh`. Also fixed `.col-list` border override specificity so the ugly divider lines are truly removed.
- **Slide 3 (S08) divider restored**: Added `flex:1` back to `.col-desc` and restored per-column `border-top` on `.col-list`. Grid `align-items` changed to `stretch` so both columns equal height — the two horizontal dividers are now aligned while remaining separate per-column lines.
- **Slide 3 (S08) tighter middle**: Moved the divider to `.col-desc` border-bottom and used `justify-content:flex-end` so desc text sits just above the divider. Reduced padding-bottom and list margin-top to `.5vh` each. This cuts the visible desc-to-list whitespace roughly in half while keeping the divider lines aligned.
- **Slide 3 (S08) revert**: Reverted the desc border-bottom / justify-content change back to list border-top with `margin-top:1vh;padding-top:1vh`. User clarified they want the description pushed UP, not down.
